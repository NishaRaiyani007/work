#include <stdio.h>
int main ()
{
    short a;
    long b;
    long long c;
    long double d;
    printf("size of sort = %d bytes\n", sizeof(a));
    printf("size of long = %d bytes\n", sizeof(b));
    printf("size of long long = %d bytes\n", sizeof(c));
    printf("size of long double = %d bytes\n", sizeof(d));
    return 0;
}
#include <stdio.h>
int main(){
    short a;
    long b;
    long long c;
    long double d;

    printf("size of sort = %d bytes\n", sizeof(a));
    printf("size of long = %d bytes\n", sizeof(b));
    printf("size of long long = %d bytes\n", sizeof(c));
    printf("size of long double = %d bytes\n", sizeof(d));

}
#include <stdio.h>
int main(){
    short a;
    long b;
    long long c;
    long double d;

    printf("size of sort = %d bytes\n", sizeof(a));
    printf("size of long = %d bytes\n", sizeof(c));
    printf("size of long long = %d bytes\n", sizeof(c));
}
#include <stdio.h>
int main()
    short a;
    long b;
    long long c;
    long double d;

    printf("size of sort = %d bytes\n", size(a));
    printf("size of long = %d bytes\n", size(b));
    printf("size of long = %d bytes\n", size(c);
    printf("size of long = %d bytes\n", size(d));

#include <stdio.h>
int main()
    short a;
    long b;
    long long c;
    long double d;

    printf("size of sort = %d bytest\n", size(a));
    printf("size of sort = %d bytest\n", size(a));
    printf("size of sort = %d bytest\n", size(c));
    printf("size of sort = %d bytest\n", size(d));
    printf("size of sort = %d bytest\n", size(b));
    printf("size of sort = %d bytest\n", size(a));

#include <stdio.h>
int main()
    short a;
    long b;
    long long c;
    long double d;

    printf("size of short = %d bytest\n", size(a));
    printf("size of sort = %d bytest\n", size(a));
    printf("size of sort = %d bytest\n", size(b));
    printf("size of sort = %d bytest\n", size(d));

#include <stdio.h>
int main()
    short a;
    long b;
    long long c;
    long double d;

    printf("size of short = %d bytest\n", size(a));
    printf("size of short = %d bytest\n", size(b));
    printf("size of short = %d bytest\n", size(c));
    printf("size of short = %d bytest\n", size(d));

#include <stdio.h>
int main()
    short a;
    long b;
    long long c;
    long double d;

    printf("size of short = %d bytest\n", size(a));
    printf("size of short = %d bytest\n", size(b));
    printf("size of short = %d bytest\n", size(c));
    printf("size of short = %d bytest\n", size(d));

#include <stdio.h>
int main()
    short a;
    long b;
    long long c;
    long double d;

    printf("size of short = %d bytest\n", size(b));
    printf("size of short = %d bytest\n", size(b));
    printf("size of short = %d bytest\n", size(c));
    printf("size of short = %d bytest\n", size(d));
    printf("size of short = %d bytest\n", size(a));

#include <stdio.h>
int main()
    short a;
    long b;
    long long c;
    long double d;
    
             

